# PostgreSQL + PostGIS (infra/docker-compose.yml)
DATABASE_URL=postgresql://uwt:uwt@localhost:5432/uwt

# API
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Web: база статического data-контракта (CDN в проде; /data = public/ в dev)
NEXT_PUBLIC_DATA_URL=/data

# ETL
# UWT_DATA_DIR=./apps/web/public/data
# UWT_LAST_DATE=2025-12-31
